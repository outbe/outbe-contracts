# Consumption Unit Signing example for TS

This example shows how sign consumption unit data in TypeScript applications.

How to run:

```shell
npm install # install dependencies
npx ts-node index.ts # run the example
```
